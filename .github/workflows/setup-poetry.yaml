name: Setup Poetry
inputs:
  python-version:
    required: false
    default: '3.13'
runs:
  using: composite
  steps:
    - name: Setup Python
      uses: actions/setup-python@v6
      with:
        python-version: ${{ inputs.python-version }}
    - name: Setup Poetry
      uses: snok/install-poetry@v1
